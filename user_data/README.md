# Private user data

This directory contains safe templates only. Before running commands that need
candidate or search-profile data, copy each required `*.example` file to the
same name without the `.example` suffix and replace every bracketed value.

The resulting `*.txt` files are intentionally ignored by Git because resumes,
testimonials, preferences, and contact details may contain sensitive personal
information. Never add real values to the example files.

